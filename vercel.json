{
    "buildCommand": "cd client && npm install && npm run build",
    "outputDirectory": "client/public",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.js"
        }
    ]
}